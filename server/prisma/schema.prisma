// --------------------------------------------
// AutoERP - Prisma Schema
// --------------------------------------------
// Database: PostgreSQL
// Author: THE FREMEN
// Hackathon: BetterHack 2025
// --------------------------------------------

generator client {
  provider = "prisma-client-js"
}



// --------------------------------------------
// ENUMS
// --------------------------------------------
enum Role {
  ADMIN
  HR
  INVENTORY
}



// --------------------------------------------
// MODELS
// --------------------------------------------

model User {
  id          String   @id @default(uuid())
  fullName    String
  email       String   @unique
  password    String
  role        Role     @default(USER)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  createdPeriods Period[]
}



model Product {
  id          String   @id @default(uuid())
  name        String
  description String?
  price       Float
  currency    Currency @relation(fields: [currencyId], references: [id])
  currencyId  String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

